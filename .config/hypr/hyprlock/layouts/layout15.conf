$cover = /tmp/hyde-mpris.png
$wall = ~/.config/hyprlock/wallpapers/10.jpg

general {
    immediate_render = true
    no_fade_in = true
    no_fade_out = true
    disable_loading_bar = false
    hide_cursor = false
    transparent = true
}


# BACKGROUND
background {
    monitor =
    path = $HOME/.cache/current-wallpaper
    blur_passes = 4
    blur_size = 2
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}



# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I")</span>"
    color = $accent
    font_size = 60
    font_family = TeachingPrintDotted-OVKlp
    position = -60, 400
    halign = center
    valign = center
}

# Time
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +":%M")</span>"
    color = $text
    font_size = 60
    font_family = TeachingPrintDotted-OVKlp
    position = 45, 400
    halign = center
    valign = center
}

# Day-Month-Date
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%A, %B %d")"
    color = $text
    font_size = 19
    font_family = TeachingPrintDotted-OVKlp
    position = 0, 340
    halign = center
    valign = center
}



# Profie-Photo
image {
    monitor =
    path = $HOME/.config/hypr/hyprlock/wallpapers/user.jpg
    border_size = 2
    border_color = $accent
    size = 190
    rounding = -1
    rotate = 0
    reload_time = -1
    reload_cmd = 
    position = -100, 0
    halign = right
    valign = center
}

 

# USER-BOX
shape {
    monitor =
    size = 200, 50
    color = $accent
    rounding = 15
    border_size = 0
    border_color = rgba(255, 255, 255, 1)
    rotate = 0
    xray = false
    position = -100, -250
    halign = right
    valign = center
}

# USER
label {
    monitor =
    # text = <i> Hi! $USER ;) </i>
    text = cmd[update:60000] echo "<i> Good $(H=$(date +%H); if [ $H -lt 12 ]; then echo Morning; elif [ $H -lt 18 ]; then echo Afternoon; else echo Evening; fi) $(echo "${USER:0:1}" | tr '[:lower:]' '[:upper:]')$(echo "${USER:1}" | tr '[:upper:]' '[:lower:]') </i>" 
    color = $text
    outline_thickness = 0
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    font_size = 11
    position = -110, -250
    halign = right
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 200, 50
    outline_thickness = 0
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(0, 0, 0, 0)
    inner_color = $accent
    font_color = $text
    fade_on_empty = false
    placeholder_text = <i><span foreground="##ffffff99">   Enter passwd</span></i>
    hide_input = false
    position = -100, -335
    halign = right
    valign = center
    rounding = 15
}




# Music blur div
shape {
    monitor =
    size = 340, 140
    color = $accent
    rounding = 30
    rotate = 0
    position = -30, -50
    halign = right
    valign = top
    zindex = 1
}



# Music album art
image {
    monitor =
    path = $cover
    size = 110
    rounding = 20
    rotate = 0
    reload_time = 0
    reload_cmd = 
    position = -40 ,-60
    halign = right
    valign = top
    zindex = 5
}


# PLAYER SOURCE
label {
    monitor =
    text = cmd[update:1000] echo "$($music --source)"
    color = $on_accent
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono 
    position = 1570, -65
    halign = left
    valign = top
    zindex = 5
}


# PLAYER TITLE
label {
    monitor =
    text = cmd[update:1000] echo "$($music --title)"
    color = $on_accent
    font_size = 12
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = 1570, -95
    halign = left
    valign = top
    zindex = 5
}



# PLAYER ARTIST
label {
    monitor =
    text = cmd[update:1000] echo "$($music --artist)"
    color = $on_accent
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mon440, 110o ExtraBold
    position = 1570, -115
    halign = left
    valign = top
    zindex = 5
}

# NAME
label {
    monitor =
    text = ⟲
    onclick = playerctl -p spotify previous
    color = $on_accent
    font_size = 25
    position = -320, -150
    halign = right
    valign = top
    zindex = 5
}

# PLAYER STATUS
label {
    monitor =
    text = cmd[update:1000] echo "$($music --status)"
    onclick = playerctl -p spotify play-pause

    color = $on_accent
    font_size = 21
    font_family = JetBrains Mono Nerd Font Mono 
    position = -270, -148
    halign = right
    valign = top
    zindex = 5
}

# NAME
label {
    monitor =
    text = »
    onclick = playerctl -p spotify next

    color = $on_accent
    font_size = 25
    position = -220, -140
    halign = right
    valign = top
    zindex = 5
}


# Battery div
shape {
    monitor =
    size = 140, 80
    color = $accent
    # color =rgb(240, 170, 170)
    rounding = 20
    rotate = 0
    position = -40, -220
    halign = right
    valign = top
    zindex = 1
}

# Battery
label {
    monitor =
    text = cmd[update:1000] echo -e "$($battery)"
    color = $on_accent
    font_size = 25
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -60, -235
    halign = right
    valign = top
    zindex = 5
}

# Weather div
shape {
    monitor =
    size = 140, 80
    color = $bg
    # color =rgb(246, 173, 85)
    rounding = 20
    rotate = 0
    position = -210, -220
    halign = right
    valign = top
    xray = false
    zindex = 1
}


# WEATHER 
label {
    monitor =
    text = cmd[update:1000] echo "$($weather)"
    color = $text
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -220, -230
    halign = right
    valign = top
    zindex = 5
}

# LOCATION 
label {
    monitor =
    text = cmd[update:1000] echo "$($location)"
    color = $text
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = -230, -260
    halign = right
    valign = top
    zindex = 5
}

